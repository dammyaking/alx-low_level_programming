#include "main.h"
/**
  *set_string - sets the value of a pointer to a char.
  *@s: pointer to pointer.
  *@so: pointer to char.
  *
  *Return: void.
  */
void set_string(char **s, char *so)
{
	*s = so;
}
